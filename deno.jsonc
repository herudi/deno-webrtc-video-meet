{
  "tasks": {
    "start": "deno run --allow-net --allow-read --no-check server.ts",
    "build": "deno run -A --no-check build.ts && deno fmt",
    "dev": "deno run --allow-net --allow-read --no-check --watch server.ts --dev"
  }
}
